import React from 'react';
import Navigation from '../Nav/navigation';


const Header = () => {
    return (
        <header>
            <Navigation/>
        </header>
    );
};

export default Header;